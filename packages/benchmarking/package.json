{
  "name": "benchmarking-scripts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "node benchmark.js",
    "bundle": "rm -f *.{tgz,zip} && ./bundle.sh"
  },
  "dependencies": {
    "webdriverio": "^9.0.6"
  },
  "engines": {
    "node": ">=18"
  },
  "bundledDependencies": [
    "webdriverio"
  ]
}
